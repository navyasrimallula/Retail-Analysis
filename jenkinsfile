pipeline {
    agent any
    environment {
        LABS = credentials("LabCreds")
    }
    stages 
    {
       
        stage('Build') {
            steps {
                sh 'pip3 install --user pipenv'
                sh '/bitnami/jenkins/home/.local/bin/pipenv -rm || exit 0'
                sh '/bitnami/jenkins/home/.local/bin/pipenv install'
                script {
                    echo "Build completed successfully"
                }
            }
        }
        stage('Test') {
            steps {
                sh '/bitnami/jenkins/home/.local/bin/pipenv run pytest'
                echo "Test completed successfully"
                }
            }
                    stage('Package') {
            steps {
                sh 'zip -r retailanalysis.zip .'
                echo "Package completed successfully"
            }
        }
        stage('Deploy') {
            steps {
                sh 'sshpass -p $LABS_PSW scp -o StrictHostKeyChecking=no -r . $LABS_USR@g01.itversity.com:/home/itv014211/retailAnalysis'
                echo "Deploy completed successfully"
            }
        }
        }
    }



